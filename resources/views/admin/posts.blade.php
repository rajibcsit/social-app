@extends('layouts.admin') @section('title','Posts') @section('content')<div class="mb-6"><h1 class="text-3xl font-black">Posts</h1><p class="text-slate-500">Review and moderate community content.</p></div><div class="space-y-4">@foreach($posts as $p)<div class="card p-5"><div class="flex justify-between gap-4"><div><div class="font-bold">{{ $p->user->name }}</div><div class="text-xs text-slate-400">{{ $p->created_at->diffForHumans() }} · {{ ucfirst($p->privacy) }}</div></div><form method="POST" action="{{ route('admin.posts.delete',$p) }}" onsubmit="return confirm('Delete this post?')">@csrf @method('DELETE')<button class="btn bg-red-50 text-red-600 text-xs">Delete</button></form></div><p class="mt-4 text-slate-700 whitespace-pre-line">{{ $p->content }}</p></div>@endforeach</div><div class="mt-5">{{ $posts->links() }}</div>@endsection
