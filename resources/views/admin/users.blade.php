@extends('layouts.admin') @section('title','Users') @section('content')<div class="mb-6"><h1 class="text-3xl font-black">Users</h1><p class="text-slate-500">Manage community accounts.</p></div><div class="card overflow-hidden"><div class="overflow-x-auto"><table class="w-full text-sm"><thead class="bg-slate-50"><tr><th class="text-left p-4">User</th><th class="text-left p-4">Joined</th><th class="text-left p-4">Status</th><th class="text-right p-4">Actions</th></tr></thead><tbody>@foreach($users as $u)<tr class="border-t border-slate-100"><td class="p-4"><div class="flex items-center gap-3"><div class="avatar">{{ strtoupper(substr($u->name,0,1)) }}</div><div><div class="font-bold">{{ $u->name }} @if($u->is_admin)<span class="text-xs text-violet-600">Admin</span>@endif</div><div class="text-xs text-slate-500">{{ $u->email }}</div></div></div></td><td class="p-4 text-slate-500">{{ $u->created_at->format('M d, Y') }}</td><td class="p-4">{{ $u->is_active?'Active':'Disabled' }}</td><td class="p-4 text-right"><form class="inline" method="POST" action="{{ route('admin.users.toggle',$u) }}">@csrf<button class="btn btn-light text-xs">{{ $u->is_active?'Disable':'Enable' }}</button></form> @if(!$u->is_admin)<form class="inline" method="POST" action="{{ route('admin.users.delete',$u) }}" onsubmit="return confirm('Delete this user?')">@csrf @method('DELETE')<button class="btn text-xs bg-red-50 text-red-600">Delete</button></form>@endif</td></tr>@endforeach</tbody></table></div><div class="p-4">{{ $users->links() }}</div></div>@endsection
