@extends('layouts.social') @section('title','Friends') @section('content')<div class="max-w-5xl mx-auto"><div class="mb-6"><h1 class="text-3xl font-black">Friends</h1><p class="text-slate-500">Manage your connections and requests.</p></div>@if($incoming->count())<div class="card p-5 mb-5"><h2 class="font-bold mb-4">Friend requests</h2><div class="grid sm:grid-cols-2 gap-3">@foreach($incoming as $f)<div class="flex items-center gap-3 border rounded-2xl p-3"><div class="avatar">{{ strtoupper(substr($f->sender->name,0,1)) }}</div><div class="flex-1"><div class="font-bold">{{ $f->sender->name }}</div><div class="text-xs text-slate-500">Wants to connect with you</div></div><form method="POST" action="{{ route('friends.accept',$f) }}">@csrf<button class="btn btn-primary">Accept</button></form><form method="POST" action="{{ route('friends.reject',$f) }}">@csrf<button class="btn btn-light">Decline</button></form></div>@endforeach</div></div>@endif<div class="card p-5"><h2 class="font-bold mb-4">Your friends · {{ $friends->count() }}</h2><div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">@forelse($friends as $friend)<div class="border rounded-2xl p-4 flex items-center gap-3"><div class="avatar">{{ strtoupper(substr($friend->name,0,1)) }}</div><div class="flex-1"><a href="{{ route('profile',$friend) }}" class="font-bold">{{ $friend->name }}</a><div class="text-xs text-emerald-600">● Connected</div></div><form method="POST" action="{{ route('friends.destroy',$friend) }}">@csrf @method('DELETE')<button class="text-xs text-slate-400 hover:text-red-500">Remove</button></form></div>@empty<div class="sm:col-span-2 lg:col-span-3 text-center py-10 text-slate-500">No friends yet. Search for people and send a request.</div>@endforelse</div></div></div>@endsection
