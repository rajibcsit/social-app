<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>@yield('title','Social')</title>@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body class="bg-slate-50 text-slate-900 min-h-screen">
<header class="sticky top-0 z-50 bg-white/90 backdrop-blur border-b border-slate-200"><div class="max-w-7xl mx-auto px-4 h-16 flex items-center gap-4">
<a href="{{ route('feed') }}" class="text-2xl font-black tracking-tight bg-gradient-to-r from-blue-600 to-violet-600 bg-clip-text text-transparent">Socially</a>
<form action="{{ route('search') }}" class="hidden md:block flex-1 max-w-md"><input name="q" value="{{ request('q') }}" placeholder="Search people..." class="w-full rounded-2xl bg-slate-100 px-5 py-2.5 outline-none focus:ring-2 focus:ring-blue-200"></form>
<nav class="ml-auto flex items-center gap-1"><a href="{{ route('feed') }}" class="nav-pill">Home</a><a href="{{ route('friends.index') }}" class="nav-pill">Friends</a><a href="{{ route('saved.index') }}" class="nav-pill">Saved</a><a href="{{ route('notifications.index') }}" class="nav-pill relative">🔔 @if(auth()->user()->unreadNotifications()->count())<span class="absolute -top-1 -right-1 text-[10px] bg-red-500 text-white rounded-full px-1.5">{{ auth()->user()->unreadNotifications()->count() }}</span>@endif</a><a href="{{ route('profile',auth()->user()) }}" class="hidden sm:block nav-pill font-semibold">{{ auth()->user()->name }}</a><form method="POST" action="{{ route('logout') }}">@csrf<button class="nav-pill">Logout</button></form></nav></div></header>
@if(session('success'))<div class="max-w-7xl mx-auto px-4 pt-4"><div class="rounded-xl bg-emerald-50 text-emerald-700 px-4 py-3">{{ session('success') }}</div></div>@endif
<main class="max-w-7xl mx-auto px-4 py-6">@yield('content')</main></body></html>
